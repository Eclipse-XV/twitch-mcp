# Smithery configuration for Twitch MCP Server (isolated build context)
projectType: docker
build:
  dockerfilePath: smithery/Dockerfile
  dockerBuildPath: smithery
  imageTag: twitch-mcp-server
deployment:
  containerPort: 8080