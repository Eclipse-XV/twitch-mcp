# Smithery configuration for Twitch MCP Server (root Dockerfile)
projectType: docker
build:
  dockerfilePath: Dockerfile
  dockerBuildPath: .
  imageTag: twitch-mcp-server
deployment:
  containerPort: 8080