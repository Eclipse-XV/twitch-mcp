# Smithery build configuration
projectType: docker
build:
  dockerfilePath: Dockerfile.smithery
  dockerBuildPath: .
  imageTag: twitch-mcp-server
deployment:
  containerPort: 8080
  # healthCheckPath: /mcp  # Optional: enable after verifying endpoint