# Smithery build configuration for Twitch MCP Server
projectType: docker
build:
  dockerfilePath: Dockerfile.smithery.minimal
  dockerBuildPath: .
  imageTag: twitch-mcp-server
deployment:
  containerPort: 8080
  # healthCheckPath: /q/health  # Enable after verifying endpoint works